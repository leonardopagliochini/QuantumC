# TODO for Paper

## 2025/08/27

### Integrate what was on slides, that seemed good to professor
    - the examples
    - the graphs on complexity (compilation complexity)
    - complexity quantum

### Change the main schema of the pipeline to a simpler one
- Simply make one that stays on one column, only simple blocks with steps, no snapshots of code or logos.

### Methods

#### QMLIR
- More details on Quantum Aware intermediate (NEW IDEA).

- Study (with graphs): how does complexity grows with different task from C?  (NEW IDEA)
    - The idea is that nobody ever studied how an IF, or nested IFs, or FORs convert in terms of complexity when going quantum
    - We might keep fixed  the representation bits (or set some values, just a few)

#### Constructs
- Must show with examples
- Must have two examples one for IF one for FOR
- Must show all steps: C, MLIR, QMLIR, QASM(circuit, not necessarily the whole circuit, the idea)

### Results

#### Complexity of Compilation Process
- Graphs: for sure one on complexity growing with number of bit for integer representation

- Study (with graphs): how does complexity grows with different task from C?  (NEW IDEA)
    - The idea is that nobody ever studied how an IF, or nested IFs, or FORs convert in terms of complexity when going quantum
    - We might keep fixed  the representation bits (or set some values, just a few)

#### Quantum Complexity
- T depth 
- depth

- Study (with graphs): how does complexity grows with different task from C?  (NEW IDEA)
    - The idea is that nobody ever studied how an IF, or nested IFs, or FORs convert in terms of complexity when going quantum
    - We might keep fixed  the representation bits (or set some values, just a few)